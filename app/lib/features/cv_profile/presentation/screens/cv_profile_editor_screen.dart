import 'package:flutter/material.dart';

class CvProfileEditorScreen extends StatelessWidget {
  const CvProfileEditorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mein CV')),
      body: const Center(child: Text('CV-Profil-Editor folgt.')),
    );
  }
}
